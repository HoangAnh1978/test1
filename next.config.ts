import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Disable Turbopack if you encounter internal errors
  // Run with: npm run dev (without --turbo flag)
};

export default nextConfig;
